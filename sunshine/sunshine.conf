# Sunshine host config — symlinked from ~/.config/sunshine/sunshine.conf
# In away mode the virtual display (DP-1) is the only active monitor and is
# primary, so it is always X11 display index 0.
capture = x11
encoder = nvenc
output_name = 0
