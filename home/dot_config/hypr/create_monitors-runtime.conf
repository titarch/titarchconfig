# Runtime monitor overrides, owned by the `stream` script (away-mode).
# chezmoi only creates this file if missing and never overwrites it.
# `stream init-virt` replaces it with a park rule at session start.
